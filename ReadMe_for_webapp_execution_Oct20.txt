사용 방법:

- VScode에서 .py 파일 실행

- VScode의 터미널 위치를 .py 파일과 동일한 폴더로 설정

- 터미널에 가상환경(venv) 가 나오지 않을 경우: ‘python3 -m venv venv’ 로 가상 환경 제작, ‘source venv/bin/activate’로 가상 환경 실행

- ‘pip install streamlit opencv-python pillow ultralytics’ 로 패키지 설치

- ‘streamlit run app.py’ 로 웹앱 실행 (자동으로 웹앱이 실행됨.)

